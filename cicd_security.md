# دمج تحليلات الأمان (SAST, SCA, DAST) في خط أنابيب CI/CD

يقدم هذا المستند تحليلاً مفصلاً ودليلاً تعليميًا حول كيفية دمج أدوات فحص الأمان الثلاثة الرئيسية (**SAST**، **SCA**، **DAST**) داخل خط أنابيب التطوير والنشر المستمر (**CI/CD Pipeline**)، لتحقيق أقصى درجات الكشف المبكر عن الثغرات (**Shift-Left Security**) مع الحفاظ على سرعة النشر.

---

## 🚀 المبدأ الأساسي: Shift-Left Security
المبدأ التوجيهي في هندسة الأمن الرقمي الحديثة (**DevSecOps**) هو نقل اختبارات الأمان إلى أبكر مرحلة ممكنة في دورة حياة تطوير البرمجيات. كلما تم اكتشاف الثغرة مبكرًا، كان حلها أسهل وأقل تكلفة، دون التسبب في تعطيل بيئة الإنتاج (**Production**).

---

## 🔍 توزيع أدوات الفحص في خط الأنابيب (CI/CD Pipeline)

### 1. تحليل SAST (اختبار أمن التطبيقات الساكن)
*   **مكان الدمج:** مرحلة **البناء والتجميع (Build / Commit Stage)** أو كـ **Pull Request Trigger**.
*   **آلية العمل:** يقوم بفحص الكود المصدري (Source Code) مباشرة دون تشغيل التطبيق للبحث عن الأنماط غير الآمنة (مثل كلمات المرور الصلبة، وجمل الاستعلام غير المعقمة).
*   **سبب الاختيار:** 
    *   يكتشف الأخطاء البرمجية أثناء كتابتها مباشرة.
    *   سريع مقارنة بالاختبارات الديناميكية.
*   **تحسين وقت النشر (Optimization):** لتجنب إبطاء عملية النشر، يمكن تشغيل فحص **SAST** خفيف وسريع على التغييرات الجديدة فقط أثناء الـ **Pull Request**، بينما يتم تشغيل فحص كامل وعميق بشكل دوري (مثلاً كل ليلة).

### 2. تحليل SCA (تحليل مكونات البرمجيات)
*   **مكان الدمج:** مرحلة **إدارة الحزم والمكتبات (Dependency / Package Stage)**.
*   **آلية العمل:** يقوم بفحص المكتبات الخارجية والمكونات مفتوحة المصدر (Dependencies) التي يعتمد عليها المشروع لمقارنتها بقواعد بيانات الثغرات المعروفة (مثل CVE).
*   **سبب الاختيار:**
    *   يمنع دمج أي مكتبة خارجية تحتوي على ثغرات معروفة قبل تجميع التطبيق النهائي.
    *   عملية سريعة جدًا وتعتمد على قراءة ملفات الإعدادات مثل `pom.xml` في Maven أو `package.json` في Node.js.

### 3. تحليل DAST (اختبار أمن التطبيقات الديناميكي)
*   **مكان الدمج:** مرحلة **ما بعد النشر في بيئة الاختبار (Post-Deployment / Staging Stage)**.
*   **آلية العمل:** يتفاعل مع التطبيق وهو قيد التشغيل الفعلي (Running Application). يقوم بمحاكاة هجمات خارجية (مثل SQLi و XSS) لمعرفة ما إذا كانت نقاط النهاية (Endpoints) قابلة للاختراق.
*   **سبب الاختيار:**
    *   لا يمكن تشغيل DAST إلا بعد بناء التطبيق بالكامل ونشره على خادم ويب (مثل Tomcat) متصل بقاعدة بيانات.
*   **تحسين وقت النشر (Optimization):** لأن فحص **DAST** يستغرق وقتًا طويلاً (قد يصل لساعات)، **لا يجب أن يعطل خط النشر الرئيسي**. يتم وضعه كعملية غير متزامنة (**Asynchronous**) في بيئة الـ **Staging**، بحيث يتم النشر، ثم يبدأ الفحص بشكل منفصل مع إرسال تقرير بالنتائج للمطورين.

---

## 📊 جدول ملخص دمج الأدوات في الـ Pipeline

| أداة الفحص | مرحلة الـ Pipeline | وقت الفحص | التأثير على وقت النشر | الهدف الرئيسي |
| :--- | :--- | :--- | :--- | :--- |
| **SAST** | Code Commit / Build | ثوانٍ إلى دقائق | منخفض (عند تخصيصه للملفات المعدلة) | فحص جودة وأمان الكود المكتوب يدويًا. |
| **SCA** | Dependency Check | ثوانٍ | منخفض جدًا | كشف الثغرات في المكتبات الخارجية المستوردة. |
| **DAST** | Post-Deploy (Staging) | دقائق إلى ساعات | معدوم (يُشغل بشكل منفصل/غير متزامن) | فحص سلوك التطبيق الفعلي ومحاكاة الهجمات الخارجية. |
